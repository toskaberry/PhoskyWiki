# 平等视角与图谱恢复

用户于 2026-09-11 确认：

- 悬停只产生临时高亮；移出节点及其详情后恢复。鼠标可进入详情阅读及操作。
- 点击画布空白或按 Escape 清除选择与聚焦，保留相机；“适应画布”同时清除聚焦并容纳整个图谱。搜索“已定位”提示同步清除。
- 保持鼠标点击、键盘 Enter 导航，以及触屏先选择再通过详情进入词条。全站、局部图谱均适用。
- 彻底删除编委会页面、其视角和历史，取消特殊角色、默认视角与公共置顶。保留其他词条、诠释者、视角及其正常历史。
- 新建词条只创建名称、简介、别名、关键文本等导航元数据；解释使用另行添加的具名诠释者视角。旧接口不得通过新建词条正文复活编委会或默默丢弃正文。
- 用户兴趣继续参与排序；没有兴趣时使用既有引用热度与稳定顺序。

数据清理以数据库迁移完成，包含审核提案、历史、相关链接和索引失效处理；只在明确隔离测试库验证。正式库执行随部署进行。
