# 视角平等与图谱恢复：最终验证

2026-09-11。实现位于 `codex/equal-perspectives-and-graph-reset`，工作目录 `D:/PhoskyWiki-worktrees/equal-perspectives`。原始 `D:/PhoskyWiki` 工作区保持干净。改动尚未提交、推送或部署，正式数据库没有执行清理。

## 完成范围

- 图谱悬停为临时状态；离开节点及详情区域后恢复。空白点击、Esc 和适应画布共享取消聚焦逻辑，同时清除搜索定位与尚未完成的定位请求；适应画布恢复整体视野。详情面板支持自然移动进入，保留触屏选择、键盘导航、拖动和缩放。
- 删除编委会特殊角色、默认解释展示以及公共视角置顶接口、控件和排序优先级。具名诠释者视角平等呈现，仍支持读者兴趣排序。
- 新词条仅含名称、别名、简介等导航元数据；解释另建具名视角。提交、导入更新和审核应用路径拒绝旧式词条正文。
- 迁移 0019 永久删除编委会页面、视角、修订及相关提案，处理依赖后删除两项旧字段；普通内容和词条讨论保留。引向被删页面的现存链接成为未解析链接。

## 验证结果

| 检查 | 结果 |
| --- | --- |
| 全量 Vitest | 293 通过，0 失败，1 跳过 |
| 全量生产构建 Playwright，关闭重试 | 95 通过，0 失败，0 flaky，1 跳过；约 2.7 分钟 |
| 最终源码 `pnpm build` | 通过，包含 TypeScript 检查 |
| 最终改动 `pnpm lint` | 通过 |
| 内容生产脚本 Node 测试 | 3 通过 |
| 容器及发布场景脚本语法检查 | 两个脚本均通过 `node --check` |
| `git diff --check` | 通过 |
| 隔离库搜索重建 | 成功，索引 436 页 |

数据库测试包括旧库升级、未标记但名为编委会的页面、改名编委会、修订及提案依赖、普通内容保留、重复迁移和 bootstrap。图谱浏览器覆盖慢速进入详情、直接离开画布、父组件重绘、延迟布局、各个清除入口、局部图谱、触屏与窄屏布局。

最终浏览器数据库是 `phosky_equal_verified_test`，PostgreSQL 位于 `127.0.0.1:55435`，测试搜索位于 `127.0.0.1:57735`。测试报告和原始日志保存在本工作目录的 `.scratch/verification/`（不提交）。

跳过项：真实 R2 对象存储契约未配置；旧有破坏性归并浏览器场景需要专用数据库前缀和显式开关，保持其保护条件。没有运行完整 Docker 发布演练。

## 复审与回归修正

应用和图谱／迁移分别经过独立复审。已修复悬停计时器因重绘取消、待定位请求重现、无法走入详情面板及直接离开画布残留悬停的问题，也删除了过期 README token 认证说明。

浏览器全量检查发现并修正三处测试数据／断言：邀请审核仍填写旧词条正文；删除目标诠释者时误删了来源视角；词条改名后误认为独立视角的标题和 URL 也应改写。前一次邀请测试超时留下测试管理员，引发审核票数连带失败；最终使用全新隔离库完整跑过，未放宽审核断言或开启重试。

## 上线要求

部署时执行迁移 0019，并完整重建搜索索引。旧应用依赖已删除字段，不能在迁移后直接回退到旧应用。此报告证明实现与隔离验证完成，不代表正式站点已删除内容。详见 ADR-0007、spec 0008 和 `docs/production.md`。
